@using Pizzeria.Web.ViewModels.SauceDips

@model ICollection<SauceDipViewModel>

@{
    ViewBag.Title = "All Restore SauceDip";
}


<h2 class="text-center">@ViewBag.Title</h2>

<table class="table">
    <thead>
        <tr>
            <th class="text-white" scope="col">SauceDip Name</th>
            <th class="text-white" scope="col">Action</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var sauceDip in Model)
        {
            <tr>

                <td class="text-white">@sauceDip.Name</td>
                <td>
                    <form class="input-group-sm" asp-route-id="@sauceDip.Id" asp-controller="SauceDips" asp-action="Edit" method="get">
                        <input type="submit" value="Update" class="fs-6 btn btn-success mb-3 p-2" />
                    </form>

                    <form class="input-group-sm" asp-route-id="@sauceDip.Id" asp-controller="SauceDips" asp-action="Restore" method="post" onclick="return confirm('Are you sure you want to restore this?')">
                        <input type="submit" value="Restore" class="fs-6 btn btn-success mb-3 p-2" />
                    </form>
                </td>

            </tr>
        }



    </tbody>
</table>
